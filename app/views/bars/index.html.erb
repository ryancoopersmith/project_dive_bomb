<h1>Bars</h1>

<div class="nav_bar">
  <%= form_tag(bars_path, method: :get) do %>
    <%= text_field_tag :term, params[:term] %>
    <%= submit_tag 'Search', name: nil %>
  <% end %>
</div>

<% @bars.each do |bar| %>
  <div class="bar_card">
    <img src="<%=bar.image_url%>" style="max-height: 175px; max-width: 175px;" >
    <p><%= bar.name %> </p>
    <p><%= bar.address %> </p>
    <p><%= bar.city%>, <%=bar.state%> <%=bar.zip%> </p>
    <p id="view_bar"><%= link_to "View Bar", bar %></p>
  </div>
<%end%>

<% if current_user %>
  <% if current_user.admin? %>
    <%=link_to "Create Bar", new_bar_path %>
  <% end %>
<% end %>

<% if user_signed_in? %>
  <img id="profile_photo" src="<%= current_user.profile_photo %>" />
<% end %>

<div id="bars"></div>
